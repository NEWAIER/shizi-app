# T03 新增依赖

| 依赖/插件 | 固定版本 | 用途 |
|---|---:|---|
| `org.jetbrains.kotlin.plugin.serialization` | 2.0.21 | 编译严格序列化模型 |
| `org.jetbrains.kotlinx:kotlinx-serialization-json` | 1.7.3 | 生产内容 JSON 严格反序列化 |
| `com.networknt:json-schema-validator` | 1.5.9 | JVM 测试真实执行 Draft 2020-12 Schema |

不存在动态版本、`latest` 或 `+`。T02 其余版本未回退。
