# PR-CONTENT-02 儿童内容模型

`ChildLearningPack` 是面向 4 岁儿童的加工层，不等同于基础汉字库。每条内容必须说明儿童释义、生活词、例句、学习目标以及图片和音频生产需求。

运行包可以同时包含基础层和 `childLearningPack`；旧包没有该字段时继续使用原有 `characters` 字段，保证历史内容兼容。

儿童内容进入正式课程前必须完成文本、媒体和家长审核。`learningGoal` 是产品目标，不直接作为儿童界面上的说明文字。
