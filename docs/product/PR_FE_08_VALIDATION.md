# PR-FE-08 真机与质量门验收记录

日期：2026-08-14

## 已完成

- 项目级 JDK 17 已接入 `gradlew.bat`，没有系统 `JAVA_HOME` 也可以启动 Gradle。
- `:app:assembleDebug` 通过，Debug APK 已生成。
- `:app:lintDebug` 通过。
- `:app:compileDebugKotlin` 通过。
- `git diff --check` 通过。

## 本阶段覆盖

- 自动学习步骤和音频结束后自动推进。
- 练习题即时判断、错误教学和暂停恢复。
- 五列字卡，单击播放、长按详情。
- 头像本地持久化、等级、星星能量和徽章展示。
- 结果页保存成功后展示成长反馈，保存失败不发放反馈奖励。

## 待连接设备执行

需要在 360×640、360×800、390×844、412×915、430×932 至少各执行一次：启动、开始学习、自动推进、退出恢复、完成结果、字卡单击/长按、头像切换和返回行为。当前环境没有已连接 Android 设备，因此未执行 `connectedDebugAndroidTest` 和截图回归。
